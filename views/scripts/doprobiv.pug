extends ../layout

block content
  if errors
    ul.alert.alert-danger
      each error,i in errors
        li
          span.glyphicon.glyphicon-fire(aria-hidden="true")
          |  #{error.msg}
  .alert.alert-success
    span.glyphicon.glyphicon-info-sign(aria-hidden="true")
    |  Слева вставляем базу, справа сайты из отчёта. На выходе получаем непробитые сайты из базы.
  form(method="post",action="/scripts/doprobiv")
    .col-xs-4
      .form-group
        label(for="left") База
        textarea.form-control(id="left",name="left")= form ? form.left : ''
    .col-xs-8
      .form-group
        label(for="right") Сайты из отчёта
        textarea.form-control(id="right",name="right")= form ? form.right : ''
      button.btn.btn-primary.pull-right(type="submit") Обработать
  script(src="/javascripts/2fields.js")
